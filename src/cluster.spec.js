/* eslint-env mocha */

import expect from 'must'

// ===================================================================

import cluster from './cluster'

// ===================================================================

describe.skip('cluster', () => {
  it('does something', () => {
    // TODO: some real tests.

    expect(cluster).to.exists()
  })
})
